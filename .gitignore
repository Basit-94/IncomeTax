node_modules
.next
out
.env*
!.env.example
*.tsbuildinfo
next-env.d.ts
.DS_Store
.vercel

# Maven build output
backend/target/
loadtest/results/

# Machine-local Claude Code permissions, not project config
.claude/settings.local.json

# Video work — hyperframes projects, renders, captures and generated frame HTML
# (base64-embedded fonts make these large). Kept on disk, out of the repo.
video/

.vercel
.env*
!.env.example

.agent-transcripts/

# Local data & SQLite databases
data/
