# Soak — not run

Status: **NOT RUN**.

The required 24-hour run at approximately 70% of the modeled peak has not been attempted. It would be irresponsible to infer memory, thread, connection, or queue stability from the 259 ms smoke window.

The future run must retain periodic correctness checks, heap/GC data, open connections, queue depth, error rate, and latency percentiles, with a raw timestamped output retained beside the report.
